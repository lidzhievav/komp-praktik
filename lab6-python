V = float(input("Введите скорость лодки (V) в м/с: "))
S = float(input("Введите расстояние между точками (S) в метрах: "))
U = float(input("Введите скорость течения реки (U) в м/с: "))

# Проверка входных данных
if V <= U:
    print("Ошибка: Скорость лодки должна быть больше скорости течения.")
    exit(1)  # Выход с кодом ошибки

# Вычисление времени движения по течению (t1)
t1 = S / (V + U)

# Вычисление времени движения против течения (t2)
t2 = S / (V - U)

# Вычисление общего времени (T)
T = t1 + t2

# Вывод результата
print(f"Общее время (T) для лодки, чтобы добраться до точки B и обратно, составляет: {T} секунд")

